# Archived migrations (pre–baseline)

These migrations were replaced by a single baseline migration: `../20260301000000_baseline.sql`.

**Do not run these.** They are kept for history only. New installs use the baseline only.

See `docs/plans-and-deltas/CLEANUP_AUDIT_POST_TEMPLATE_ONLY.md` for context.
